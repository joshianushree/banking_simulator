# ================================
# ? DATABASE CONFIGURATION
# ================================
spring.datasource.url=jdbc:mysql://localhost:3306/banking_simulator?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=system
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# We're using JdbcTemplate, so no need for full JPA auto-DDL
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# ================================
# ? EMAIL (GMAIL SMTP)
# ================================
mail.sender.email=anushreejoshi689@gmail.com
mail.sender.password=lsqkcoygauhdxvsa

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${mail.sender.email}
spring.mail.password=${mail.sender.password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ================================
# ? TWILIO (SMS OTP)
# ================================
twilio.account-sid=
twilio.auth-token=
twilio.phone-number=+14793493515

# ================================
# ? OTP CONFIGURATION
# ================================
banking.otp.expiry-minutes=5

# ================================
# ? LOGGING & GENERAL
# ================================
logging.level.root=INFO
logging.level.org.springframework.jdbc.core=INFO
spring.main.allow-bean-definition-overriding=true
server.port=8080

# ================================
# ? FILE PATHS FOR REPORTS (optional)
# ================================
app.reports.path=reports/
app.reports.accounts=${app.reports.path}accounts.pdf
app.reports.transactions=${app.reports.path}transactions.pdf
